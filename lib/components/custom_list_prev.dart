import 'package:flutter/material.dart';

class CustomListPrev extends StatelessWidget {
  const CustomListPrev({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('custom list prev'),
    );
  }
}
